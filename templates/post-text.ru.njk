<img alt="Курсы Рубля (RUB) на {{ dateloc }}" src="{{ imgsrc }}">
<p>Центральный Банк России состоянием на {{ dateloc }} установил следующие курсы Рубля (RUB) по отношению к мировым валютам:</p>

{% for feature in features %}{% if feature.code=='USD/RUB' or feature.code=='EUR/RUB' or feature.code=='GBP/RUB'
or feature.code=='RUB/RUB' %}<p>{{ feature.code }} ({{ feature.currency }}) {{ feature.rate }}</p> {% endif %}{% endfor %}

<table class="table-finance">
<thead>
<tr>
<th>Код</th>
<th>Единицы</th>
<th>Курс</th>
<th class="mob_hidden">Дата</th>
<th class="mob_desksm_hidden">Название</th>
</tr>
</thead>
<tbody>
{% for feature in features %}
<tr>
<td>{{ feature.code }}</td><td>{{ feature.amount|default('1')}}</td><td>{{ feature.rate }}</td>
<td class="mob_hidden">{{ dateloc2 }}</td><td class="mob_desksm_hidden">{{ feature.currency | upper }}</td>
</tr>
{% endfor %}
</tbody>
</table>
<p>Эксперты БиБаБо (Bbabo.net) отслеживают все изменения на валютном рынке России</p>